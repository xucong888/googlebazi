# DeepSeek API Key — 仅在服务端使用，绝不放入 VITE_ 前缀（否则会泄露到前端 bundle）
DEEPSEEK_API_KEY="your_deepseek_api_key_here"

# Firebase Config — firebase-applet-config.json 已包含，这里无需重复配置
# 如需覆盖可在此填写
# VITE_FIREBASE_API_KEY=""

# 服务端口（默认 3000）
PORT=3000

# 本地开发时 Vite 也会读取以下变量用于 HMR 配置
# DISABLE_HMR=false
